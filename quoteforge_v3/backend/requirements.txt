# Pinned to match the locally-developed/tested versions so the Docker
# image reproduces the same environment (older FastAPI enforced a stricter
# 204-no-body assertion that the app was never tested against).
fastapi==0.135.3
starlette==1.0.0
uvicorn[standard]==0.43.0
sqlalchemy==2.0.36
alembic==1.14.0
pydantic[email]==2.12.5
pydantic-settings==2.13.1
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
# Pin bcrypt: passlib 1.7.4's init self-test hashes an 80-byte secret, which
# bcrypt>=4.1 rejects ("password cannot be longer than 72 bytes"). 4.0.1
# still truncates silently and matches the locally-tested version.
bcrypt==4.0.1
python-multipart==0.0.19
reportlab==4.2.5
python-docx==1.1.2
# HTML master template → PDF rendering.
# svglib pinned to <1.5 so we don't pull rlpycairo → pycairo → Cairo C
# library (which would require libcairo2-dev on Render and `brew install
# cairo pkg-config` locally). 1.4.x renders SVG via reportlab directly,
# which is enough for our template needs and keeps the image slim.
jinja2==3.1.4
svglib==1.4.1
xhtml2pdf==0.2.16
httpx==0.28.1
openai==1.58.1
chromadb==0.5.23
langchain==0.3.13
langchain-openai==0.3.0
langchain-community==0.3.13
aiosmtplib==3.0.2
email-validator==2.2.0
aiosqlite==0.20.0
# Postgres async driver (Render). SQLAlchemy picks aiosqlite vs asyncpg
# based on the DATABASE_URL scheme — see app/core/database.py.
asyncpg==0.30.0
# Fernet symmetric encryption for Salesforce OAuth tokens at rest.
cryptography==44.0.0
# Module 6 — Deal Insights (win probability predictor)
lightgbm==4.5.0
shap==0.46.0
pandas==2.2.3
anthropic==0.96.0
apscheduler==3.11.2
